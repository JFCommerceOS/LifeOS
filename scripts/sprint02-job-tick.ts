/**
 * Placeholder worker tick for Hardening Sprint 02 — claims `ScheduledJob` rows and marks
 * `SyncOutbox` as APPLIED when a full sync transport exists. Wire to cron or a dedicated
 * `apps/worker` process when multi-device sync ships.
 */
console.info('[sprint02-job-tick] No-op placeholder. Use API scheduled jobs + diagnostics until worker runtime is added.');
