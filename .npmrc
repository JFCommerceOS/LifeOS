# Prisma + pnpm on Windows: hoist engine packages so paths are stable and locks are less likely.
# After changing this file, run: `pnpm install` (or delete `node_modules` then install).
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/client
